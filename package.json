{
  "name": "skyo-prediction",
  "version": "0.1.0",
  "private": true,
  "description": "Browser-only precipitation nowcasting via optical-flow extrapolation of radar imagery.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "start": "npx http-server public -p 3000 -c-1",
    "build": "node build.js",
    "test": "node --test test/*.test.js",
    "test:coverage": "node --test --experimental-test-coverage test/*.test.js",
    "check:coverage": "node scripts/check-coverage.mjs"
  }
}
